services:
  - type: web
    name: trampo-backend
    runtime: node
    rootDir: artifacts/api-server
    buildCommand: corepack enable && pnpm install && pnpm run build
    startCommand: node dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production

  - type: web
    name: trampo-frontend
    runtime: static
    rootDir: artifacts/trampo
    buildCommand: corepack enable && pnpm install && pnpm run build
    staticPublishPath: dist
